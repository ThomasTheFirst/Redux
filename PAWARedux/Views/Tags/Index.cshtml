@model IEnumerable<PAWARedux.Models.Tags>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TagName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstDateTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UseCount)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TagName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FirstDateTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UseCount)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TagsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TagsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TagsID })
        </td>
    </tr>
}

</table>
